import React from "react";

const Principal = ({ id, name, email }) => {
  return (
    <div>
      <div>
        <div>Principal Join Id: {id}</div>
        <div>Princiapl Name: {name}</div>
        <div>Princiapl Email: {email}</div>
        <h3 />
      </div>
    </div>
  );
};

export default Principal;
